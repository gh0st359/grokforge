global:
  scrape_interval: 10s
  evaluation_interval: 15s

scrape_configs:
  - job_name: grokforge-core
    static_configs:
      - targets: ["core:9090"]

  - job_name: grokforge-agents
    metrics_path: /metrics
    static_configs:
      - targets: ["agents:8001"]
